<form method="post" action="{{ path('app_best_ones_delete', {'id': best_one.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ best_one.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
