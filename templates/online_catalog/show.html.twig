{% extends 'base.html.twig' %}
{% block title %}Online Catalog n° {{onlinecatalog.id }}{% endblock %}

{% block body %}
    <h1>Online Catalog {{onlinecatalog.member }}</h1>


    <table class="table">
        <tbody>
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Link</th>
            </tr>
            </thead>
            {% for tvshows in onlinecatalog.getTvshow %}

            <tr>
                <td>{{ tvshows.id }}</td>
                <td>{{ tvshows.name }}</td>
                <td><a href="{{ path('app_tv_show_show', {'id': tvshows.id}) }}">show</a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

        <a href="{{ path('app_tv_show_new', {'id': onlinecatalog.id}) }}" class="btn btn-primary">Add a new show</a>


<a href="{{ path('app_member_show', {'id': onlinecatalog.member.id})  }}" class="btn btn-secondary">back to member</a>
{% endblock %} {# body #} 
