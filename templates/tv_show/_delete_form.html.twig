<form method="post" action="{{ path('app_tv_show_delete', {'id': tv_show.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tv_show.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
